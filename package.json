{
  "name": "s2ktux-terminal-validation",
  "private": true,
  "type": "module",
  "scripts": {
    "test:unit": "node tests/run-all.mjs",
    "test:contracts": "node tests/terminal-contracts.mjs && node tests/terminal-command-coverage.mjs",
    "test:e2e": "npm run build && playwright test",
    "test": "npm run build && npm run test:unit && playwright test",
    "build": "node scripts/build-production.mjs"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "esbuild": "0.28.2",
    "prettier": "^3.9.6"
  }
}
